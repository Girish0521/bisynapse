# BISynapse Backend (Render) — Environment Variables
# Set these in the Render dashboard under your service's Environment tab

# Express port (Render sets this automatically, keep 4000 for local dev)
PORT=4000

# Node environment
NODE_ENV=production

# Your Vercel frontend URL (used for CORS)
# Example: https://bisynapse.vercel.app
FRONTEND_URL=
